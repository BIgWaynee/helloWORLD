#include "CalVecGpu.h"
//
//int BasicDem::cGpuCal::sum_Matrix() {
	//int a[3] = { 1.1 , 2.2 , 3.3 };
	//int b[3] = { 1.0 , 1.0 , 1.0 };
	////cudaMallocHost(&c, 3 * sizeof(double) );
	//cudaHostAlloc((void**)&c, sizeof(double) * 3, cudaHostAllocDefault);

	//cudaMalloc((void**)&d_a, sizeof(double) * 3);
	//cudaMalloc((void**)&d_b, sizeof(double) * 3);
	//cudaMalloc((void**)&d_a, sizeof(double) * 3);

	//cudaMemcpy(d_a, a, sizeof(double) * 3, cudaMemcpyHostToDevice);
	//cudaMemcpy(d_b, b, sizeof(double) * 3, cudaMemcpyHostToDevice);
	//cudaMemcpy(d_c, c, sizeof(double) * 3, cudaMemcpyHostToDevice);
	//int dimx = 1;
	//int dimy = 3;

//}